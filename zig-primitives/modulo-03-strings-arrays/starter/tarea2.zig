const std = @import("std");

pub fn main() void {
    // Daily temperatures for a week
    const temperaturas = [_]i32{ 22, 25, 18, 30, 35, 28, 20 };

    // TODO: Find the maximum temperature
    // Hint: Start with the first element as the maximum
    // var max: i32 = ...

    // TODO: Find the minimum temperature
    // var min: i32 = ...

    // TODO: Calculate the sum to later compute the average
    // var suma: i32 = 0;

    // TODO: Use a for loop to find max, min, and sum
    // for (temperaturas) |temp| {
    //     ...
    // }

    // TODO: Calculate the average as a float
    // Hint: Convert suma and length to f64
    // const promedio: f64 = ...

    // TODO: Print results
    // Expected: max=35, min=18, promedio~= 25.43
    // std.debug.print("Temperatura maxima: {}\n", .{max});
    // std.debug.print("Temperatura minima: {}\n", .{min});
    // std.debug.print("Promedio: {d:.2}\n", .{promedio});
}
