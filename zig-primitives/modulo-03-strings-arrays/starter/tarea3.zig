const std = @import("std");

pub fn main() void {
    const texto = "programacion";

    // TODO: Count the vowels in the string
    // Vowels are: a, e, i, o, u
    // var vocales: u32 = 0;
    // for (texto) |c| {
    //     ...
    // }

    // TODO: Check if the string contains the letter 'z'
    // var tiene_z: bool = false;
    // ...

    // TODO: Print the string in uppercase
    // Hint: Uppercase letters are 32 less than lowercase in ASCII
    // 'a' = 97, 'A' = 65, difference = 32
    // std.debug.print("En mayusculas: ", .{});
    // for (texto) |c| {
    //     // Only convert lowercase letters (a-z are 97-122)
    //     ...
    // }
    // std.debug.print("\n", .{});

    // TODO: Print results
    // std.debug.print("Cantidad de vocales: {}\n", .{vocales});
    // std.debug.print("Contiene 'z': {}\n", .{tiene_z});
}
